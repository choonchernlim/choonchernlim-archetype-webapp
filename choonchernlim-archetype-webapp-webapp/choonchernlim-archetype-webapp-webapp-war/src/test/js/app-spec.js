'use strict';

describe( 'app', function () {

    it( 'a really stupid test', function () {
        expect( true ).toBe( true );
    } );

    // TODO delete
    it( 'a really stupid test', function () {
        var app = require( 'app' );
        console.log( app );

        expect( true ).toBe( true );
    } );

} );