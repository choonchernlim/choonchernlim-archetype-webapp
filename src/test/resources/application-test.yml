
# Disable any logging
logging.level.root: OFF

spring:
  datasource:
    initialize: true
    platform: h2
    url: jdbc:h2:mem:test;MODE=MSSQLServer;DATABASE_TO_UPPER=false

  jpa.properties.hibernate.dialect: org.hibernate.dialect.H2Dialect
